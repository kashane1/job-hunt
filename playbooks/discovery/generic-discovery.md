# Generic Discovery Playbook

1. Start from approved source sites in `config/sources.yaml`
2. Reuse the current tab when possible
3. Save one raw lead source artifact per opportunity
4. Normalize immediately into the lead schema
5. Dedupe before scoring

